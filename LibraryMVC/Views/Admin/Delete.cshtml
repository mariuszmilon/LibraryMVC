@model LibraryMVC.Models.UserDto

@{
    ViewData["Title"] = "Delete";
}

<h1>Deleting...</h1>

<h3>Are you sure you want to delete this user?</h3>

<div>
    <hr />
    <div class="d-flex">
        <div class="w-50">
            <dl class="row">
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.UserName)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.UserName)
                </dd>
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.LastName)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.LastName)
                </dd>
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.Email)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.Email)
                </dd>
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.Role)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.Role)
                </dd>
            </dl>
            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                    <div class="mt-3">
                        <button class="btn btn-dark my-3 me-3 px-3 py-1">
                            <a class="link-light text-decoration-none" asp-action="Index">Back to List</a>
                        </button>
                        <input type="submit" value="Delete" class="btn btn-danger px-3" />
                    </div>
            </form>
        </div>
    </div>
</div>
